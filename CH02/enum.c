#include<stdlib.h>
#include<stdio.h>
#include<stdint.h>
#include<stdbool.h>

enum day {MON=1, TUE, WED, THU, FRI, SAT, SUN};

int main() {

	if(true) {
		printf("%d\n", SUN);
	}
	
	return EXIT_SUCCESS;
}
